# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
trigger:
- main

pool:
  name: Default

steps:
- script: |
    echo "Stock Dashboard CI Pipeline Started"
    echo "Repository: stock-dashboard"
    echo "Branch: main"
  displayName: 'Run basic CI test'